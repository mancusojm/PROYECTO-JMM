import cart from './assets/shopping-cart.png'

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt="cart-widget"style={{width: '2%'}}/>
            0
        </div>

    )
        
    
}

export default CartWidget